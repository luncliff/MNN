file(GLOB_RECURSE MNN_PLUGIN_SRCS "${CMAKE_CURRENT_SOURCE_DIR}/*.cpp")

add_library(MNN_Plugin ${MNN_PLUGIN_SRCS})

install(TARGETS MNN_Plugin
        EXPORT MNNConfig
        RUNTIME DESTINATION bin LIBRARY DESTINATION lib ARCHIVE DESTINATION lib
)
